<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Compose uygulaması temasını yönetir; burada yalnızca temel köprü tanımlanır. -->
    <style name="Theme.KefoBaba" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@color/bg_dark</item>
        <item name="android:navigationBarColor">@color/bg_dark</item>
        <item name="android:windowBackground">@color/bg_dark</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <!-- Splash (Android 12+ SplashScreen API) -->
    <style name="Theme.KefoBaba.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/black</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/logo_basak</item>
        <item name="postSplashScreenTheme">@style/Theme.KefoBaba</item>
    </style>

</resources>
